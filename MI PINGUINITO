<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>¿Serás mi Valentín? 🐧❤️</title>
    <style>
        body {
            text-align: center;
            font-family: 'Arial', sans-serif;
            background: url('https://i.imgur.com/YtCwdK9.png') no-repeat center center/cover;
            color: #d63384;
            margin: 0;
            padding: 0;
            overflow: hidden;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        h1 {
            font-size: 36px;
            background: rgba(255, 255, 255, 0.8);
            padding: 10px 20px;
            border-radius: 15px;
        }
        .buttons {
            margin-top: 20px;
        }
        button {
            font-size: 20px;
            padding: 10px 20px;
            margin: 10px;
            border: none;
            cursor: pointer;
            border-radius: 10px;
            transition: 0.3s;
        }
        .yes {
            background-color: #ff4d6d;
            color: white;
        }
        .yes:hover {
            background-color: #d63384;
        }
        .no {
            background-color: #6c757d;
            color: white;
            position: absolute;
        }
        #hearts {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }
    </style>
</head>
<body>

    <h1>¿Quieres ser mi Valentín? 🐧💖</h1>
    <div class="buttons">
        <button class="yes" onclick="showMessage()">Sí 💖</button>
        <button class="no" id="noButton" onmouseover="moveNoButton()">No 😢</button>
    </div>

    <canvas id="hearts"></canvas>

    <script>
        function showMessage() {
            document.body.innerHTML = "<h1>¡Sabía que dirías que sí! 🐧💕</h1><p>Eres mi pingüinito favorito, te amo mucho! 😘</p><img src='https://i.imgur.com/fn9k49P.png' width='300'>";
        }

        function moveNoButton() {
            let button = document.getElementById("noButton");
            let x = Math.random() * (window.innerWidth - 100);
            let y = Math.random() * (window.innerHeight - 50);
            button.style.left = x + "px";
            button.style.top = y + "px";
        }

        // Animación de corazones
        const canvas = document.getElementById("hearts");
        const ctx = canvas.getContext("2d");
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        let hearts = [];
        function createHeart() {
            let x = Math.random() * canvas.width;
            let y = canvas.height + 10;
            let size = Math.random() * 10 + 5;
            let speed = Math.random() * 3 + 1;
            hearts.push({x, y, size, speed});
        }

        function drawHearts() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            hearts.forEach((heart, index) => {
                ctx.fillStyle = "#ff4d6d";
                ctx.beginPath();
                ctx.arc(heart.x, heart.y, heart.size, 0, Math.PI * 2);
                ctx.fill();
                heart.y -= heart.speed;
                if (heart.y < -10) hearts.splice(index, 1);
            });
            requestAnimationFrame(drawHearts);
        }

        setInterval(createHeart, 300);
        drawHearts();
    </script>

</body>
</html>
